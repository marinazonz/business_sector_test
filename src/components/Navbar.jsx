import NavbarSearch from "./NavbarSearch.";

const Navbar = () => {
    return (
        <section className='w-full '>
            <NavbarSearch />
        </section>
    );
};

export default Navbar;
